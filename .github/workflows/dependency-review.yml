name: Dependency Review

# This workflow scans pull requests for dependency changes
# and alerts on security vulnerabilities or license issues

on:
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    name: Review Dependencies
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          # Fail on high or critical vulnerabilities
          fail-on-severity: high
          
          # Allow these licenses (permissive open source licenses)
          allow-licenses: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD
          
          # Comment on PR with results
          comment-summary-in-pr: always
          
          # Show vulnerability details
          vulnerability-check: true
          
          # Show license details
          license-check: true
          
          # Configuration file (optional)
          # config-file: '.github/dependency-review-config.yml'
      
      - name: Upload dependency graph
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dependency-review-results
          path: dependency-review-*.json
          retention-days: 30
